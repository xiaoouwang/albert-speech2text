# Required for server-side / Worker auth (optional if you paste the key in the UI)
ALBERT_API_KEY=

# Defaults shown below — change only if you use another Albert environment
ALBERT_BASE_URL=https://albert.api.etalab.gouv.fr/v1
PORT=3001

# Production frontend (GitHub Pages) — public Worker URL only
# VITE_API_BASE=https://albert-speech2text.singerxo.workers.dev
